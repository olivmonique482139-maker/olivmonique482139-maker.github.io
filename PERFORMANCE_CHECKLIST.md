# 猫小灶每次迭代强制验收清单

本清单适用于之后每一次视觉、互动、内容或功能迭代。未全部通过时不得发布长期公网版本。

## 1. 自动质量门禁

- 运行 `npm run check`。
- JavaScript 语法必须通过。
- 所有运行时资源必须存在。
- 禁用角色资产引用必须为 0。
- 首屏视频不超过 750 KB；待机视频不超过 100 KB；背景音乐不超过 1.5 MB。
- 不允许重新加入阻塞式 Google Fonts 或其他第三方字体请求。

## 2. 本地真实浏览器回归

- 桌面端 1440×1000：首屏、问卷、结果、购物袋、404、导航与音乐。
- 手机端 390×844：无横向溢出，按钮可点击，弹窗可滚动。
- 控制台错误和警告均为 0。
- 猫爪鼠标在页面、问卷和购物袋弹窗中都可见。
- 页面离屏或切换标签后，视频能够暂停；返回后按用户状态恢复。

## 3. 音乐专项

- 清空缓存后的第一次点击必须显示“音乐加载中…”，并在可播放后自动开始。
- 快速连续点击三次不得造成按钮状态与真实播放状态不一致。
- 播放、暂停、再次播放各检查一次。
- 模拟慢速网络时，播放器必须显示缓冲或超时提示，不能无反馈。

## 4. 发布后线上复验

- 检查长期链接返回 HTTP 200。
- 检查版本号、首屏海报、视频、音乐和 2D 角色资源均已刷新。
- 在线重复桌面端、手机端和音乐专项测试。
- 记录本次提交号和验收结果后，才向用户报告完成。
